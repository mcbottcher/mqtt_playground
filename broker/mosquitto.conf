# Mosquitto configuration for local playground

# Listen on localhost only
listener 1883 127.0.0.1
protocol mqtt

# Allow anonymous connections
allow_anonymous true

# Log to stdout
log_dest stdout
log_type all
